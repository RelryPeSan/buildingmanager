/*import firebase from '../services/firebase';

export default {
  async index(data) {
    const equipamentos = await firebase.database().ref('equipamentos');
    const resp = await equipamentos.orderByKey();

    return resp;
  },
};
//*/
